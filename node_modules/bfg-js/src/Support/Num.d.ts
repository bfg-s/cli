export interface NumInterface {
    isNumber(num: any): boolean;
}
export declare class Num implements NumInterface {
    /**
     * Check is number
     * @param num
     */
    isNumber(num: any): boolean;
}
